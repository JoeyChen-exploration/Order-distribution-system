# 项目待办清单（TODO）

更新时间：2026-04-24

## P0（下一步优先）

- [ ] 在派单算法中加入“航班延误风险因子”（面向未来订单）
  - 目标：针对 T+1（例如明天）订单，在冲突检测与推荐评分中引入风险缓冲。
  - V1：先用规则法（不依赖外部 API）输出 `riskLevel`（low/medium/high）和 `bufferMinutes`（如 10/25/45）。
  - V2：接入外部数据（天气/机场运行/航班动态）后升级为概率化风险评估。
  - 落地位置建议：`lib/dispatch-engine.ts`、`app/api/flights/*`、`README.md`。

## P1（近期优化）

- [ ] 派单链路增加“二次确认快照”与失败重试（避免批量确认时个别订单失败）
- [ ] 为派单核心函数补充回归测试（车型兼容、冲突检测、MRV 顺序）
- [ ] 登录限流从内存版升级为持久化/分布式（多实例一致）
- [ ] 去掉 `AUTH_SESSION_SECRET` 的开发默认值，未配置时启动告警

## P2（体验与运营）

- [ ] 司机工作时段支持“软缓冲区”（例如超出 15 分钟仅降分，不是硬排除）
- [ ] 增加“预排-复排”双阶段调度（T-24 粗排，T-6 复排）
- [ ] 导入结果页增加“可修复错误的一键重试导入”
